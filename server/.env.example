# Rename this file to `.env` and adjust the values for your environment.
# Anything defined here will be loaded by src/config/env.js when the server starts.

# Core server settings
PORT=5000

# MongoDB connection string (e.g., your MongoDB Atlas URI)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/<database-name>?retryWrites=true&w=majority

# Allowed CORS origins (comma-separated). Leave blank to allow all origins in development.
CORS_ORIGINS=http://localhost:5173

# JWT configuration
JWT_SECRET=super-secret-key-change-me
JWT_EXPIRES_IN=7d
